#include "Student.h"

Student::Student(std::string firstName, std::string lastName, Specialization specialization, int recBookNumber) : _firstName(firstName), _lastName(lastName), _specialization(specialization), _recBookNumber(recBookNumber)
{

}
